@model BacPeBune.Models.Question

<h1>Question</h1>
<p>@Model.Text</p>

<form method="post" action="@Url.Action("Answer", "Quiz", new { quizId = Model.QuizID })">
    <fieldset>
        <legend>Select an answer:</legend>
        @if (ViewBag.Answers != null && ((List<BacPeBune.Models.Answer>)ViewBag.Answers).Any())
        {
            foreach (var answer in (List<BacPeBune.Models.Answer>)ViewBag.Answers)
            {
                <div>
                    <input type="radio" id="answer_@answer.AnswerID" name="selectedAnswer" value="@answer.AnswerID" />
                    <label for="answer_@answer.AnswerID">@answer.Text</label>
                </div>
            }
        }
        else
        {
            <p>No answers available for this question.</p>
        }
    </fieldset>
    @if(ViewBag.IsLastQuestion){
        <button type="submit">Submit Answer</button>
    }
    
</form>

<div class="navigation-buttons">
    @if ((int)ViewBag.QuestionIndex > 0)
    {
        <a href="@Url.Action("Show", "Quiz", new { quizId = Model.QuizID, questionIndex = (int)ViewBag.QuestionIndex - 1 })" class="btn btn-secondary">Previous</a>
    }

    @if( (int)ViewBag.QuestionIndex < ViewBag.Questions.Count - 1){
        <a href="@Url.Action("Show", "Questions", new {quizId = Model.QuizID, questionIndex = (int)ViewBag.QuestionIndex + 1})"></a>
    }
</div>

@if (ViewBag.QuestionIndex != null && ViewBag.Questions != null)
{
    <p>Question @((int)ViewBag.QuestionIndex + 1) of @ViewBag.Questions.Count</p>
}